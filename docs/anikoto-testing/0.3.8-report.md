# AniKoto testing 0.3.8 — 2026-09-08

Testing-only publication requested by the repository owner. Not a stable release.

- ID/family: `anikoto-testing`, separate from installed `anikoto`.
- Display name: `AniKoto (BETA - Testing only)`.
- Version: `0.3.8`.
- Manifest SHA-256: `b8de4757a4ce7c3b2e1b0af3fd01d37f03838a33489545f68083811e91abe7eb`.
- The tested resolution change is `/stream/getSources` → `/stream/getSourcesNew`.
- No JavaScript, app code, new schema capability, security relaxation or additional host was introduced.
- Test publication changes only candidate identity/display/release notes relative to the previously tested prototype. Parsed behavior equality was verified.

## Evidence

The behavior-identical prototype passed 45 HTTP media checks from the frozen
50-title corpus (40 development, 10 withheld); five title matches were unverified,
none of the 45 matched titles failed media resolution. Overall verdict PARTIAL.
Corpus seed: 20260817. Corpus SHA-256:
`d9c309abfcd4712aaf878de86027711a421caa9aaeacf0559dc47c3a278c6ed5`.

Fragrant Flower episode 1 SUB and DUB separately passed macOS AVPlayer startup
and middle seeking. Native caption parsing yielded 335 SUB / 323 DUB English
cues. Beginning/middle/end segment samples for both variants decoded H.264/AAC
at 1080p without decoder errors. The isolated regression suite executed 377 tests,
17 intentional skips, zero failures. Dedicated opt-in prototype suite: 3 passed.
Native source baseline: `f7288766c39a7c3f19ceca679d34d00e84d012d7`.

Before this test publication, the exact renamed manifest passed a fresh native
`certify-title` run for Fragrant Flower (middle episode, SUB, HTTP HLS-segment
proof, 1,442 seconds). A separate native test validated the entire unsigned index
and cross-checked both manifests and checksums. HiAnime's existing entry was
compared unchanged. `git diff --check` passed.

These are not 45 physical-device playback tests. Spoken language, subtitle
rendering/synchronization, sustained iPhone playback, downloads, offline playback
and AirPlay remain unverified. The test collection requires unsigned-collection
support; it is not the distribution channel for older signed-only app versions.

## Test steps

1. Refresh `kas021/Testing-Modules-AR` and add the newly offered source.
2. Select **AniKoto (BETA - Testing only)** and search for Fragrant Flower.
   Avoid opening saved items associated with the normal AniKoto source.
3. Play episode 1 in SUB and DUB; select English captions and confirm they appear.
4. Seek to the middle and near the end; watch continuously for 5–10 minutes.
5. Test a download separately, then airplane-mode playback and seeking.

To stop testing, select the normal AniKoto source. No production repository,
existing HiAnime manifest, installed app or saved library was modified by this
publication. No private key, stream URL, token, transcript or media recording is
included in the repository.
